import Canvas from '../Canvas/Canvas';
// import Preloader from '../UI/Preloader/Preloader';

const Main = () => {
  return (
    <main className="main">
      <Canvas />
      {/* <Preloader /> */}
    </main>
  );
};

export default Main;
